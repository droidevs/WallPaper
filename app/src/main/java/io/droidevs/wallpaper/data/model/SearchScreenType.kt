package io.droidevs.wallpaper.data.model

/**
 * Defines the different screens in the app where a user can perform a search.
 * This enum is used to categorize search history entries.
 */
enum class SearchScreenType {
    AlbumList,
    LocalWallpaperList,
    CollectionList,
    FavoritesList,
    TopicList,
    All
}